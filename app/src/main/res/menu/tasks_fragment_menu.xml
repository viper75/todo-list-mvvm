<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/search_action"
        android:title="Search"
        android:icon="@drawable/ic_search"
        app:actionViewClass="androidx.appcompat.widget.SearchView"
        app:showAsAction="always"/>

    <item
        android:title="Sort"
        android:icon="@drawable/ic_sort"
        app:showAsAction="always">

        <menu>

            <item
                android:id="@+id/sort_by_name_action"
                android:title="Sort by name"/>

            <item
                android:id="@+id/sort_by_date_created_action"
                android:title="Sort by date created"/>
        </menu>
    </item>

    <item
        android:id="@+id/hide_completed_tasks_action"
        android:checkable="true"
        android:title="Hide completed"
        app:showAsAction="never"/>

    <item
        android:id="@+id/delete_all_completed_tasks_action"
        android:title="Delete all completed"
        app:showAsAction="never"/>
</menu>